{
  "name": "electron-scada-react",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "cd ../scada-react && npm run build && cd ../electron-scada-react && (if exist builder rd /s /q builder) && mkdir builder && cd .. && xcopy /s .\\scada-react\\build\\ .\\electron-scada-react\\builder\\ && cd ./electron-scada-react && electron-builder"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^31.3.1",
    "electron-builder": "^24.13.3",
    "electron-is-dev": "^3.0.1"
  },
  "dependencies": {
  }
}
